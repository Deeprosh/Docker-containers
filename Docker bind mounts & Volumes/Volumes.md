# Volumes
Using Docker commands, we can create volumes, which is the logical partition creating on the host. Volumes have long Lifecycle.
~~~
docker -v is used when you want to parse all things
docker --mount is used to elaborate the details
~~~
